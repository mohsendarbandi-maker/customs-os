import React,{useEffect,useState}from'react';import{Loader2,Ship,AlertTriangle,ChevronDown,ChevronUp,ArrowRight}from'lucide-react';import{Link}from'react-router-dom';import{supabase}from'../lib/supabase';import{buildCargoName}from'../lib/cargoNaming';
const CHECKLIST=['در انتظار مبلغ ترخیصیه','پاس کشتی','اظهار','مالیات علی الحساب','درخواست ضمانتنامه حقوق ورودی و ارزش افزوده','ارزیابی','آزمایشگاه','مجوز استاندارد','نوبت کارشناسی','کد ساتا','تبصره دو منطقه آزاد'];
const STAGES=[{key:'start',label:'شروع عملیات'},{key:'warehouse',label:'ورود اطلاعات قبض انبار'},{key:'declaration',label:'ثبت اظهار'},{key:'customs',label:'عملیات گمرکی'},{key:'exit',label:'درب خروج'}];
const stageIndex=(x:any)=>x.decl?.workflow_stage===5?5:x.decl?.workflow_stage===4?4:(x.current_status==='epl_submitted'||x.current_status==='kottaj_received')?3:x.current_status==='documents_ready'?2:1;
const stageTarget=(x:any,i:number)=>{if(i===1)return'/operations';if(i===2)return`/operations?tab=pre-declaration&shipmentId=\${encodeURIComponent(x.id)}`;if(i===3)return x.decl?`/operations?tab=declaration&shipmentId=\${encodeURIComponent(x.id)}`:`/operations?tab=declaration&shipmentId=\${encodeURIComponent(x.id)}`;if(i===4)return x.decl?`/operations?tab=stage5&declarationId=\${encodeURIComponent(x.decl.id)}&shipmentId=\${encodeURIComponent(x.id)}`:'/operations';return x.decl?`/operations?tab=exit&declarationId=\${encodeURIComponent(x.decl.id)}&shipmentId=\${encodeURIComponent(x.id)}`:'/operations'};
export const LatestShipmentStatuses:React.FC=()=>{const[data,setData]=useState<any[]>([]);const[loading,setLoading]=useState(true);const[selected,setSelected]=useState<Record<string,number|null>>({});useEffect(()=>{(async()=>{try{const{data:s,error:e}=await supabase.from('shipments').select('id,bill_of_lading_no,vessel_id,client_id,cargo_count,cargo_count_unit,current_status,transport_documents_status,release_invoice_payment_status,updated_at,created_at').order('updated_at',{ascending:false}).limit(12);if(e)throw e;const rows=s||[],vids=rows.map((x:any)=>x.vessel_id).filter(Boolean),cids=rows.map((x:any)=>x.client_id).filter(Boolean),ids=rows.map((x:any)=>x.id);const[{data:vs},{data:cs},{data:ds}]=await Promise.all([vids.length?supabase.from('vessels').select('id,name').in('id',vids):Promise.resolve({data:[]}),cids.length?supabase.from('clients').select('id,name').in('id',cids):Promise.resolve({data:[]}),ids.length?supabase.from('customs_declarations').select('id,shipment_id,workflow_stage,created_at').in('shipment_id',ids).order('created_at',{ascending:false}):Promise.resolve({data:[]})]);const vm=new Map((vs||[]).map((x:any)=>[x.id,x.name||''])),cm=new Map((cs||[]).map((x:any)=>[x.id,x.name||''])),dm=new Map<string,any>();(ds||[]).forEach((x:any)=>{if(!dm.has(x.shipment_id))dm.set(x.shipment_id,x)});const declIds=(ds||[]).map((x:any)=>x.id);const{data:ci}=declIds.length?await supabase.from('declaration_checklist_items').select('declaration_id,item_key,completed').in('declaration_id',declIds):{data:[]};const cm2=new Map<string,any[]>();(ci||[]).forEach((x:any)=>{if(!cm2.has(x.declaration_id))cm2.set(x.declaration_id,[]);cm2.get(x.declaration_id)!.push(x)});setData(rows.map((x:any)=>{const decl=dm.get(x.id)||null;const items=decl?cm2.get(decl.id)||[]:[];const completed=CHECKLIST.filter(k=>items.some((i:any)=>i.item_key===k&&i.completed)).length;const next=CHECKLIST.find(k=>!items.some((i:any)=>i.item_key===k&&i.completed))||null;return {...x,owner:cm.get(x.client_id)||'صاحب کالا نامشخص',vessel:vm.get(x.vessel_id)||'کشتی نامشخص',decl,checkCompleted:completed,next}}))}catch{setData([])}finally{setLoading(false)}})()},[]);return <section dir="rtl" className="mt-4 rounded-3xl border app-border bg-[var(--surface)] p-4 md:p-5"><div className="flex items-center gap-2 mb-4"><Ship size={18}/><h2 className="font-black">وضعیت محموله‌ها</h2></div>{loading?<div className="py-8 text-center app-muted"><Loader2 className="animate-spin mx-auto"/></div>:!data.length?<div className="py-8 text-center app-muted text-sm">هنوز محموله‌ای ثبت نشده است.</div>:<div className="space-y-3">{data.map(x=>{const current=stageIndex(x),open=selected[x.id]??null,warnings=[...(x.current_status==='completed'&&x.transport_documents_status!=='ready'?['اسناد حمل و قبض انبار آماده نیست']:[]),...(x.current_status==='completed'&&x.release_invoice_payment_status!=='paid'?['صورتحساب ترخیصیه پرداخت نشده']:[])];return <div key={x.id} className="rounded-2xl border app-border bg-[var(--surface-2)] p-4"><div className="flex flex-wrap justify-between gap-2"><div><div className="font-black text-sm">{buildCargoName(x.cargo_count,x.cargo_count_unit,x.owner,x.vessel)}</div><div className="mt-1 text-xs app-muted">B/L: {x.bill_of_lading_no||'—'} · {x.owner} · {x.vessel}</div></div><span className="text-xs font-black px-2.5 py-1 rounded-full bg-[var(--primary)] text-white">مرحله {current} از ۵</span></div><div className="grid grid-cols-5 gap-1 mt-4">{STAGES.map((s,i)=>{const n=i+1,active=n===current,isOpen=open===n;return <button type="button" key={s.key} onClick={()=>setSelected(v=>({...v,[x.id]:isOpen?null:n}))} className={`rounded-xl px-1.5 py-2 text-[10px] leading-4 border transition ${active?'bg-[var(--primary)] text-white border-[var(--primary)] font-black':'app-border app-muted'} ${isOpen?'ring-2 ring-[var(--primary)]/30':''}`}><span className="block font-black">{n}</span>{s.label}</button>})}</div>{warnings.length>0&&<div className="mt-3 space-y-2">{warnings.map(w=><div key={w} className="flex items-center gap-2 rounded-xl border border-amber-500/40 bg-amber-500/10 px-3 py-2 text-xs font-bold text-amber-700 dark:text-amber-300"><AlertTriangle size={15}/>{w}</div>)}</div>}{open!==null&&<div className="mt-3 rounded-2xl border app-border bg-[var(--surface)] p-3"><div className="flex items-center justify-between gap-2"><b className="text-sm">{STAGES[open-1].label}</b><button type="button" onClick={()=>setSelected(v=>({...v,[x.id]:null}))} className="text-xs app-muted inline-flex items-center gap-1"><ChevronUp size={14}/> بازگشت به وضعیت کلی</button></div><div className="mt-3 grid sm:grid-cols-2 gap-2 text-xs"><div className="rounded-xl border app-border px-3 py-2"><span className="app-muted">وضعیت فعلی:</span> <b>{open===current?'مرحله فعلی':'مرحله ثبت‌شده'}</b></div>{open===2&&<div className="rounded-xl border app-border px-3 py-2"><span className="app-muted">اسناد پایه:</span> <b>{x.transport_documents_status==='ready'?'موجود و آماده اظهار':'هنوز آماده نیست'}</b></div>}{open===3&&<div className="rounded-xl border app-border px-3 py-2"><span className="app-muted">اظهار:</span> <b>{x.decl?'اظهار ثبت شده':'هنوز ثبت نشده'}</b></div>}{open===4&&<div className="rounded-xl border app-border px-3 py-2"><span className="app-muted">چک‌لیست:</span> <b>{x.decl?`\${x.checkCompleted} مورد انجام شده`:'اظهار ثبت نشده'}</b></div>}{open===5&&<div className="rounded-xl border app-border px-3 py-2"><span className="app-muted">درب خروج:</span> <b>{x.decl?.workflow_stage===6?'تکمیل شده':'در انتظار عملیات'}</b></div>}{open===2&&x.current_status==='completed'&&x.release_invoice_payment_status!=='paid'&&<div className="rounded-xl border border-amber-500/40 px-3 py-2 text-amber-700 dark:text-amber-300"><b>هشدار:</b> صورتحساب ترخیصیه پرداخت نشده</div>}</div><Link onClick={()=>{try{localStorage.setItem('customs_registered_shipment',JSON.stringify({id:x.id,updatedAt:new Date().toISOString()}))}catch{}}} to={stageTarget(x,open)} className="mt-3 inline-flex items-center gap-1 rounded-xl bg-[var(--primary)] px-3 py-2 text-xs font-bold text-white"><ArrowRight size={14}/> ورود به این مرحله</Link></div>}{open===null&&<button type="button" onClick={()=>setSelected(v=>({...v,[x.id]:current}))} className="mt-3 inline-flex items-center gap-1 text-xs font-bold text-[var(--primary)]">جزئیات مرحله فعلی <ChevronDown size={14}/></button>}</div>})}</div>}</section>};