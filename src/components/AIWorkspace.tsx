import React,{useRef,useState} from 'react';
import {Bot,FileSearch,Loader2,Paperclip,Send,X,CheckCircle2} from 'lucide-react';
import {supabase} from '../lib/supabase';

type Msg={role:'user'|'assistant';text:string};
type Extracted=Record<string,string>;

const keyLabels:Record<string,string[]>={client:['صاحب کالا','گیرنده','خریدار','buyer','consignee'],seller:['فروشنده','صادرکننده','seller','exporter'],regNumber:['ثبت سفارش','registration order'],regDate:['تاریخ ثبت سفارش','registration order date'],shippingLine:['کشتیرانی','shipping line','carrier'],vesselName:['کشتی','نام کشتی','vessel'],billOfLading:['b/l','بارنامه','bill of lading'],billYear:['سال بارنامه','bill year'],voyageNo:['voyage','سفر'],originPort:['مبدأ بارگیری','بندر مبدأ','origin port'],destinationPort:['بندر مقصد','مقصد','destination port'],originCountry:['کشور مبدأ','origin country'],transactionCountry:['کشور معامله','transaction country'],deliveryTerm:['اینکوترمز','incoterm','شرایط تحویل'],cargoDescription:['شرح کالا','کالا','cargo description','goods'],cargoCount:['تعداد','quantity','qty'],cargoCountUnit:['واحد','unit'],netWeight:['وزن خالص','net weight'],grossWeight:['وزن ناخالص','gross weight'],tariffCode:['hs','تعرفه','hs code'],invoiceAmount:['مبلغ فاکتور','ارزش فاکتور','invoice amount','invoice value'],invoiceCurrency:['ارز فاکتور','ارز','currency'],insuranceIrr:['بیمه ریالی','insurance'],dutyRate:['حقوق ورودی','نرخ حقوق','duty rate'],unloadingDate:['تاریخ تخلیه','unloading date'],warehouseReceiptNo:['شماره قبض انبار','warehouse receipt'],warehouseReceiptDate:['تاریخ قبض انبار','warehouse receipt date']};
const normalize=(s:string)=>s.toLowerCase().replace(/[\u200c\u200f]/g,' ').replace(/[^\p{L}\p{N}]+/gu,' ').trim();
function applyToCurrentForm(fields:Extracted){let applied=0;const labels=Array.from(document.querySelectorAll('label')) as HTMLLabelElement[];const setNative=(el:HTMLInputElement|HTMLSelectElement|HTMLTextAreaElement,value:string)=>{const proto=el instanceof HTMLSelectElement?HTMLSelectElement.prototype:el instanceof HTMLTextAreaElement?HTMLTextAreaElement.prototype:HTMLInputElement.prototype;const desc=Object.getOwnPropertyDescriptor(proto,'value');desc?.set?.call(el,value);el.dispatchEvent(new Event(el instanceof HTMLSelectElement?'change':'input',{bubbles:true}));el.dispatchEvent(new Event('change',{bubbles:true}));};Object.entries(fields).forEach(([field,value])=>{if(!value)return;const needles=keyLabels[field];if(!needles)return;const label=labels.find(l=>needles.some(n=>normalize(l.textContent||'').includes(normalize(n))));if(!label)return;let el:HTMLInputElement|HTMLSelectElement|HTMLTextAreaElement|null=null;if(label.htmlFor)el=document.getElementById(label.htmlFor) as any;if(!el)el=label.parentElement?.querySelector('input,select,textarea') as any;if(!el)el=label.nextElementSibling?.querySelector?.('input,select,textarea') as any;if(el&&!el.disabled){setNative(el,String(value));applied++;}});return applied;}
const explainInvokeError=async(error:any)=>{try{if(error?.context?.json){const body=await error.context.json();if(body?.error)return String(body.error);if(body?.message)return String(body.message);}if(error?.context?.text){const body=await error.context.text();if(body)return body;}if(error?.message&&error.message!=='Edge Function returned a non-2xx status code')return error.message;}catch{}return 'Edge Function پاسخ موفق نداد.';};
const fileToBase64=(file:File)=>new Promise<string>((resolve,reject)=>{const r=new FileReader();r.onload=()=>{const s=String(r.result||'');const comma=s.indexOf(',');resolve(comma>=0?s.slice(comma+1):s)};r.onerror=()=>reject(new Error('خواندن فایل ناموفق بود.'));r.readAsDataURL(file);});

export const AIWorkspace:React.FC<{pageContext?:string}>=({pageContext=''})=>{const[open,setOpen]=useState(false),[tab,setTab]=useState<'chat'|'doc'>('chat'),[input,setInput]=useState(''),[messages,setMessages]=useState<Msg[]>([]),[busy,setBusy]=useState(false),[fileName,setFileName]=useState(''),[fields,setFields]=useState<Extracted>({}),[status,setStatus]=useState(''),[applied,setApplied]=useState(0),[interactionId,setInteractionId]=useState(()=>{try{return localStorage.getItem('customs_ai_interaction_id')||''}catch{return ''}});const fileRef=useRef<HTMLInputElement|null>(null);

const ask=async(text=input.trim(),documentText='',extractFields=false,documentData='',documentMimeType='')=>{if(!text&&!documentText&&!documentData)return;setBusy(true);setStatus('');if(text)setMessages(m=>[...m,{role:'user',text}]);setInput('');try{const prompt=extractFields?`از سند زیر اطلاعات قابل انتقال به فرم‌های Customs OS را استخراج کن. فقط JSON معتبر و بدون markdown برگردان. کلیدهای مجاز: client,seller,regNumber,regDate,shippingLine,vesselName,billOfLading,billYear,voyageNo,originPort,destinationPort,originCountry,transactionCountry,deliveryTerm,cargoDescription,cargoCount,cargoCountUnit,netWeight,grossWeight,tariffCode,invoiceAmount,invoiceCurrency,insuranceIrr,dutyRate,unloadingDate,warehouseReceiptNo,warehouseReceiptDate. فقط مقادیری را برگردان که صریحاً در سند وجود دارند؛ حدس نزن. همه مقادیر string باشند.`:(text||'این سند را برای عملیات گمرکی و لجستیکی تحلیل کن و اطلاعات مهم، خطاها، مغایرت‌ها و فیلدهای قابل استخراج را ارائه بده.');const{data,error}=await supabase.functions.invoke('ai-assistant',{body:{query:prompt,document_text:extractFields?'':documentText,document_data:documentData||undefined,document_mime_type:documentMimeType||undefined,extract_fields:extractFields,page_context:pageContext,previous_interaction_id:extractFields?undefined:(interactionId||undefined)}});if(error)throw error;const answer=typeof data?.answer==='string'?data.answer:(data?.answer?JSON.stringify(data.answer):data?.message||'پاسخ دریافت نشد.');if(data?.interaction_id&&!extractFields){setInteractionId(data.interaction_id);try{localStorage.setItem('customs_ai_interaction_id',data.interaction_id)}catch{}}if(!extractFields)setMessages(m=>[...m,{role:'assistant',text:answer}]);if(extractFields){let parsed:Extracted={};try{const raw=answer.replace(/^```(?:json)?/i,'').replace(/```$/,'').trim();const x=JSON.parse(raw);Object.entries(x||{}).forEach(([k,v])=>{if(typeof v==='string'&&v.trim())parsed[k]=v.trim();});}catch{throw new Error('پاسخ ساختاریافته AI قابل پردازش نبود.')}setFields(parsed);setStatus(Object.keys(parsed).length?`${Object.keys(parsed).length} فیلد از سند استخراج شد.`:'سند خوانده شد اما فیلد قابل انتقالی پیدا نشد.');try{localStorage.setItem('customs_ai_extracted',JSON.stringify({fields:parsed,fileName,pageContext,at:new Date().toISOString()}))}catch{}}}catch(e:any){const detail=await explainInvokeError(e);setStatus(detail);if(text)setMessages(m=>[...m,{role:'assistant',text:`خطای دستیار: ${detail}`}])}finally{setBusy(false)}};

const extract=async(file:File)=>{setFileName(file.name);setFields({});setApplied(0);setBusy(true);setStatus('در حال ارسال مستقیم سند به Gemini…');try{if(!file.type==='application/pdf' as any && !file.type.startsWith('image/'))throw new Error('فعلاً PDF و تصویر پشتیبانی می‌شود.');if(file.size>10*1024*1024)throw new Error('برای ارسال مستقیم، حجم فایل باید حداکثر ۱۰ مگابایت باشد.');const base64=await fileToBase64(file);await ask('', '', true, base64, file.type); }catch(e:any){setStatus(e?.message||'خطا در آپلود سند')}finally{setBusy(false)}};

const apply=()=>{const n=applyToCurrentForm(fields);setApplied(n);setStatus(n?`${n} فیلد از سند روی فرم فعلی اعمال شد. قبل از ذخیره کنترل کنید.`:'فیلد متناظر در فرم فعلی پیدا نشد؛ اطلاعات استخراج‌شده ذخیره شده است.')};
return <>{!open&&<button onClick={()=>setOpen(true)} className="fixed left-4 bottom-16 z-[55] flex items-center gap-2 rounded-2xl bg-[var(--primary)] text-white px-4 py-3 shadow-2xl font-bold text-sm"><Bot size={18}/>دستیار هوشمند</button>}{open&&<div className="fixed left-4 bottom-4 z-[70] w-[min(440px,calc(100vw-32px))] h-[min(700px,calc(100vh-32px))] rounded-3xl border app-border bg-[var(--surface)] shadow-2xl overflow-hidden flex flex-col" dir="rtl"><header className="p-4 border-b app-border flex items-center justify-between"><div className="flex items-center gap-2"><span className="app-brand-mark"><Bot size={18}/></span><div><b>Customs AI</b><div className="text-[10px] app-muted">Gemini 3.5 Flash-Lite · Interactions API</div></div></div><button className="icon-btn" onClick={()=>setOpen(false)}><X size={17}/></button></header><div className="p-2 border-b app-border grid grid-cols-2 gap-2"><button onClick={()=>setTab('chat')} className={`rounded-xl p-2 text-xs font-bold ${tab==='chat'?'bg-[var(--primary)] text-white':'bg-[var(--surface-2)]'}`}><Bot size={14} className="inline ml-1"/>چت</button><button onClick={()=>setTab('doc')} className={`rounded-xl p-2 text-xs font-bold ${tab==='doc'?'bg-[var(--primary)] text-white':'bg-[var(--surface-2)]'}`}><FileSearch size={14} className="inline ml-1"/>استخراج سند</button></div>{tab==='chat'?<><div className="flex-1 overflow-auto p-3 space-y-3">{messages.length===0&&<div className="rounded-2xl bg-[var(--surface-2)] p-4 text-sm leading-7">در هر بخش سامانه سؤال بپرسید یا از گیره کاغذ برای آپلود سند استفاده کنید.</div>}{messages.map((m,i)=><div key={i} className={`rounded-2xl p-3 text-sm leading-7 whitespace-pre-wrap ${m.role==='user'?'bg-[var(--primary)] text-white mr-8':'bg-[var(--surface-2)] ml-4'}`}>{m.text}</div>)}{busy&&<div className="text-xs app-muted flex items-center gap-2"><Loader2 size={14} className="animate-spin"/>در حال پردازش…</div>}</div><div className="p-3 border-t app-border flex gap-2"><button className="icon-btn shrink-0" onClick={()=>{setTab('doc');setTimeout(()=>fileRef.current?.click(),0)}} title="آپلود سند"><Paperclip size={17}/></button><input value={input} onChange={e=>setInput(e.target.value)} onKeyDown={e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();ask()}}} placeholder="سؤال خود را بپرسید…" className="flex-1 rounded-xl border app-border bg-[var(--surface-2)] px-3 text-sm outline-none"/><button onClick={()=>ask()} disabled={busy||!input.trim()} className="icon-btn bg-[var(--primary)] text-white disabled:opacity-40"><Send size={16}/></button></div></>:<div className="flex-1 overflow-auto p-4"><input ref={fileRef} type="file" accept="application/pdf,image/*" className="hidden" onChange={e=>{const f=e.target.files?.[0];if(f)extract(f)}}/><button onClick={()=>fileRef.current?.click()} disabled={busy} className="w-full rounded-2xl border border-dashed app-border p-8 text-center bg-[var(--surface-2)]"><FileSearch className="mx-auto mb-3" size={30}/><b>PDF یا تصویر را آپلود کنید</b><div className="text-xs app-muted mt-2">ارسال مستقیم فایل به Gemini · PDF یا تصویر · حداکثر ۱۰MB</div></button>{fileName&&<div className="mt-4 rounded-xl bg-[var(--surface-2)] p-3 text-sm">{fileName}</div>}{status&&<div className="mt-4 rounded-xl border app-border p-3 text-xs leading-6">{status}</div>}{Object.keys(fields).length>0&&<><div className="mt-4 rounded-xl border app-border p-3"><div className="text-sm font-bold mb-2 flex items-center gap-2"><CheckCircle2 size={16}/>فیلدهای قابل انتقال: {Object.keys(fields).length}</div><div className="max-h-40 overflow-auto text-xs leading-6">{Object.entries(fields).map(([k,v])=><div key={k} className="flex gap-2"><span className="app-muted min-w-28">{k}</span><span>{v}</span></div>)}</div></div><button onClick={apply} disabled={busy} className="mt-3 w-full rounded-xl bg-[var(--primary)] text-white p-3 text-sm font-bold">اعمال روی فرم فعلی</button>{applied>0&&<div className="mt-2 text-xs text-emerald-400">{applied} فیلد اعمال شد.</div>}</>}{fileName&&<button onClick={()=>fileRef.current?.click()} disabled={busy} className="mt-3 w-full rounded-xl border app-border p-3 text-sm font-bold">انتخاب سند دیگر</button>}</div>}</div>}</>;
};
