import React,{useEffect,useMemo,useState} from 'react';
import {ArrowRight,FileText,Printer,Receipt} from 'lucide-react';
import {Link,useParams} from 'react-router-dom';
import {supabase} from '../lib/supabase';
const money=(n:number)=>new Intl.NumberFormat('fa-IR').format(Math.round(Number(n)||0));
export const FinanceShipmentPage:React.FC=()=>{const{id}=useParams();const[s,setS]=useState<any>();const[c,setC]=useState<any>();const[costs,setCosts]=useState<any[]>([]);const[cats,setCats]=useState<any[]>([]);const[invoices,setInvoices]=useState<any[]>([]);const[msg,setMsg]=useState('');useEffect(()=>{(async()=>{if(!id)return;const[{data:sh,error},{data:co},{data:ca},{data:iv}]=await Promise.all([supabase.from('shipments').select('*').eq('id',id).single(),supabase.from('finance_cost_items').select('*').eq('shipment_id',id).order('occurred_at',{ascending:false}),supabase.from('finance_cost_categories').select('id,name_fa'),supabase.from('finance_invoice_shipments').select('invoice_id,finance_invoices(id,invoice_no,status,total_amount,currency,issue_date)').eq('shipment_id',id)]);if(error)setMsg(error.message);setS(sh);setCosts(co||[]);setCats(ca||[]);setInvoices((iv||[]).map((x:any)=>x.finance_invoices).filter(Boolean));if(sh?.client_id){const{data:cl}=await supabase.from('clients').select('id,name').eq('id',sh.client_id).maybeSingle();setC(cl)}})()},[id]);const total=useMemo(()=>costs.filter(x=>x.status!=='cancelled').reduce((a,x)=>a+Number(x.amount_irr||0)+Number(x.vat_amount||0),0),[costs]);return <main className="space-y-5" dir="rtl"><div className="flex items-center gap-3"><Link to="/finance" className="icon-btn"><ArrowRight size={18}/></Link><div><h1 className="text-2xl font-black">مالی محموله</h1><p className="app-muted">{s?.display_name||s?.bill_of_lading_no||'—'} · {c?.name||'—'}</p></div><button onClick={()=>window.print()} className="mr-auto px-3 py-2 rounded-xl bg-[var(--primary)] text-white"><Printer size={15} className="inline ml-1"/>چاپ</button></div>{msg&&<div className="app-surface border app-border rounded-xl p-3">{msg}</div>}<div className="grid md:grid-cols-4 gap-3"><Metric title="صاحب کالا" value={0} text={c?.name}/><Metric title="B/L" value={0} text={s?.bill_of_lading_no}/><Metric title="تعداد" value={0} text={(s?.cargo_count||'—')+' '+(s?.cargo_count_unit||'')}/><Metric title="هزینه کل" value={total}/></div><section className="app-surface border app-border rounded-2xl p-4"><h2 className="font-bold mb-4"><Receipt className="inline ml-1" size={18}/>هزینه‌ها</h2><table className="w-full text-sm"><thead><tr className="border-b app-border"><th className="p-2 text-right">شرح</th><th>دسته</th><th>مبلغ</th><th>صورتحساب</th><th>وضعیت</th></tr></thead><tbody>{costs.map(x=><tr key={x.id} className="border-b app-border/50"><td className="p-2">{x.description}</td><td>{cats.find(z=>z.id===x.category_id)?.name_fa||'—'}</td><td dir="ltr">{money(Number(x.amount_irr)+Number(x.vat_amount||0))} IRR</td><td>{x.billable?'بله':'خیر'}</td><td>{x.status}</td></tr>)}</tbody></table></section><section className="app-surface border app-border rounded-2xl p-4"><h2 className="font-bold mb-4"><FileText className="inline ml-1" size={18}/>صورتحساب‌های مرتبط</h2>{invoices.map(i=><div key={i.id} className="flex justify-between py-3 border-b app-border"><span>{i.invoice_no}</span><span>{money(i.total_amount)} {i.currency} · {i.status}</span></div>)}{!invoices.length&&<div className="app-muted">صورتحسابی ثبت نشده است.</div>}</section></main>};
const Metric=({title,value,text}:{title:string,value:number,text?:string})=><div className="app-surface border app-border rounded-2xl p-4"><span className="text-xs app-muted">{title}</span><b className="block mt-2">{text||money(value)+' ریال'}</b></div>;