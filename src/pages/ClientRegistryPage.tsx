import React,{useEffect,useState} from 'react';
import {ArrowRight,Building2,Copy,Eye,EyeOff,Loader2,Printer,Save,ShieldCheck,Trash2} from 'lucide-react';
import {Link} from 'react-router-dom';
import {supabase} from '../lib/supabase';
const onlyDigits=(v:string)=>v.replace(/[^0-9]/g,'');
export const ClientRegistryPage:React.FC=()=>{const[clients,setClients]=useState<any[]>([]);const[form,setForm]=useState({name:'',nationalId:'',eplUsername:'',eplPassword:''});const[selectedId,setSelectedId]=useState('');const[showPassword,setShowPassword]=useState(true);const[saving,setSaving]=useState(false);const[deleting,setDeleting]=useState(false);const[message,setMessage]=useState('');
 const load=async()=>{const{data}=await supabase.from('clients').select('id,name,national_id,created_at').order('created_at',{ascending:false});setClients(data||[])};useEffect(()=>{load();try{const x=JSON.parse(localStorage.getItem('customs_last_client')||'null');if(x?.id){setSelectedId(x.id);setForm(p=>({...p,name:x.name||p.name,nationalId:x.nationalId||p.nationalId,eplUsername:x.eplUsername||p.eplUsername}))}}catch{}},[]);
 const save=async()=>{const username=onlyDigits(form.eplUsername);if(!form.name.trim()||!form.nationalId.trim()||!username||!form.eplPassword)return setMessage('نام شرکت، شناسه ملی شرکت، شناسه ملی اظهارکننده و رمز EPL الزامی است.');setSaving(true);setMessage('');try{let id=selectedId;if(id){const{error}=await supabase.from('clients').update({name:form.name.trim(),national_id:form.nationalId.trim()}).eq('id',id);if(error)throw error}else{const{data,error}=await supabase.rpc('create_client_workflow',{p_name:form.name.trim(),p_national_id:form.nationalId.trim()});if(error)throw error;id=data}const{error}=await supabase.rpc('save_client_epl_credentials',{p_client_id:id,p_epl_username:username,p_epl_password:form.eplPassword});if(error)throw error;setSelectedId(id);setForm(p=>({...p,eplUsername:username}));try{localStorage.setItem('customs_last_client',JSON.stringify({id,name:form.name.trim(),nationalId:form.nationalId.trim(),eplUsername:username}))}catch{}setMessage('اطلاعات صاحب کالا و ورود EPL ذخیره شد.');await load()}catch(e:any){setMessage(e?.message||'خطای ثبت')}finally{setSaving(false)}};
 const selectClient=async(id:string)=>{const c=clients.find(x=>x.id===id);if(!c)return;setSaving(true);try{const{data,error}=await supabase.rpc('get_client_epl_credentials',{p_client_id:id});if(error)throw error;setSelectedId(id);setForm({name:c.name||'',nationalId:c.national_id||'',eplUsername:data?.[0]?.epl_username||'',eplPassword:data?.[0]?.epl_password||''});setShowPassword(true);try{localStorage.setItem('customs_last_client',JSON.stringify({id,name:c.name||'',nationalId:c.national_id||'',eplUsername:data?.[0]?.epl_username||''}))}catch{}setMessage('')}catch(e:any){setMessage(e?.message||'خطای دریافت اطلاعات EPL')}finally{setSaving(false)}};
 const remove=async()=>{const c=clients.find(x=>x.id===selectedId);if(!selectedId)return;if(!window.confirm(`هشدار حذف کامل «${c?.name||'صاحب کالا'}»\n\nداده‌های عملیاتی وابسته حذف می‌شوند. Audit records و Status histories غیرقابل حذف و غیرقابل تغییر باقی می‌مانند.\n\nادامه می‌دهید؟`))return;setDeleting(true);try{const{error}=await supabase.rpc('delete_client_workflow',{p_client_id:selectedId});if(error)throw error;setSelectedId('');setForm({name:'',nationalId:'',eplUsername:'',eplPassword:''});localStorage.removeItem('customs_last_client');setMessage('صاحب کالا حذف شد؛ Audit و Status History دست‌نخورده باقی ماندند.');await load()}catch(e:any){setMessage(e?.message||'حذف انجام نشد')}finally{setDeleting(false)}};
 const copy=(v:string,l:string)=>navigator.clipboard?.writeText(v).then(()=>setMessage(`${l} کپی شد.`));
 return <main className="min-h-screen bg-slate-950 text-slate-100 p-5 md:p-8" dir="rtl"><div className="max-w-6xl mx-auto"><div className="flex items-center justify-between gap-4 mb-6"><div><h1 className="text-2xl font-black flex items-center gap-2"><Building2 className="text-blue-400"/> ثبت صاحب کالا</h1><p className="text-sm text-slate-400 mt-2">اطلاعات شرکت و ورود EPL برای استفاده در کشتیرانی، عملیات و چاپ اظهار.</p></div><Link to="/" className="px-4 py-2 rounded-xl border border-slate-700 bg-slate-900 text-sm"><ArrowRight className="inline ml-2" size={16}/> صفحه اصلی</Link></div><div className="grid lg:grid-cols-[1.35fr_1fr] gap-6"><section className="bg-slate-900 border border-slate-800 rounded-2xl p-6"><div className="grid md:grid-cols-2 gap-4"><label><span className="text-xs text-slate-400">نام شرکت *</span><input value={form.name} onChange={e=>setForm(p=>({...p,name:e.target.value}))} className="mt-1 w-full bg-slate-950 border border-slate-800 rounded-xl px-3 py-3"/></label><label><span className="text-xs text-slate-400">شناسه ملی شرکت *</span><input inputMode="numeric" value={form.nationalId} onChange={e=>setForm(p=>({...p,nationalId:onlyDigits(e.target.value)}))} dir="ltr" className="mt-1 w-full bg-slate-950 border border-slate-800 rounded-xl px-3 py-3"/></label><label><span className="text-xs text-slate-400">شناسه ملی اظهارکننده / User EPL *</span><div className="flex gap-2 mt-1"><input inputMode="numeric" value={form.eplUsername} onChange={e=>setForm(p=>({...p,eplUsername:onlyDigits(e.target.value)}))} dir="ltr" className="flex-1 bg-slate-950 border border-slate-800 rounded-xl px-3 py-3"/><button type="button" onClick={()=>copy(form.eplUsername,'شناسه EPL')} className="px-3 rounded-xl border border-slate-700"><Copy size={17}/></button></div></label><label><span className="text-xs text-slate-400">رمز عبور سامانه EPL *</span><div className="flex gap-2 mt-1"><div className="relative flex-1"><input type={showPassword?'text':'password'} value={form.eplPassword} onChange={e=>setForm(p=>({...p,eplPassword:e.target.value}))} dir="ltr" className="w-full bg-slate-950 border border-slate-800 rounded-xl px-3 py-3 pl-11"/><button type="button" onClick={()=>setShowPassword(v=>!v)} className="absolute left-3 top-3 text-slate-400">{showPassword?<EyeOff size={18}/>:<Eye size={18}/>}</button></div><button type="button" onClick={()=>copy(form.eplPassword,'رمز EPL')} className="px-3 rounded-xl border border-slate-700"><Copy size={17}/></button></div></label></div><div className="mt-4 text-xs text-slate-400"><ShieldCheck className="inline ml-1" size={15}/> رمز از Vault خوانده می‌شود و برای کاربر مجاز قابل مشاهده و کپی است.</div>{message&&<div className="mt-4 p-3 rounded-xl bg-slate-950 border border-slate-800 text-sm">{message}</div>}<div className="mt-5 flex flex-wrap gap-2"><button onClick={save} disabled={saving||deleting} className="px-6 py-3 rounded-xl bg-blue-600 font-bold">{saving?<Loader2 className="inline animate-spin ml-2" size={17}/>:<Save className="inline ml-2" size={17}/>} {selectedId?'ذخیره تغییرات':'ثبت صاحب کالا'}</button>{selectedId&&<><button onClick={()=>window.print()} className="px-5 py-3 rounded-xl border border-slate-700"><Printer className="inline ml-2" size={17}/> چاپ اطلاعات ورود</button><button onClick={remove} disabled={deleting} className="px-5 py-3 rounded-xl border border-red-800 text-red-300"><Trash2 className="inline ml-2" size={17}/> حذف کامل</button></>}</div></section><section className="bg-slate-900 border border-slate-800 rounded-2xl p-6"><h2 className="font-bold mb-4">صاحبان کالا</h2><div className="space-y-2 max-h-[520px] overflow-auto">{clients.length===0?<p className="text-sm text-slate-500">هنوز صاحب کالایی ثبت نشده است.</p>:clients.map(c=><button key={c.id} onClick={()=>selectClient(c.id)} className={`w-full text-right p-3 rounded-xl border ${selectedId===c.id?'border-blue-500 bg-blue-950/30':'border-slate-800 bg-slate-950/50'}`}><div className="font-bold">{c.name}</div><div className="text-xs text-slate-500 mt-1">شناسه ملی: {c.national_id||'—'}</div></button>)}</div></section></div><section className="hidden print:block bg-white text-black mt-6 p-8"><h2 className="text-2xl font-black">Customs OS — اطلاعات ورود سامانه EPL</h2><div className="grid grid-cols-2 gap-6 mt-8"><div><b>صاحب کالا</b><div>{form.name}</div></div><div><b>شناسه ملی شرکت</b><div dir="ltr">{form.nationalId}</div></div><div><b>شناسه ملی اظهارکننده / User EPL</b><div dir="ltr">{form.eplUsername}</div></div><div><b>رمز EPL</b><div dir="ltr">{form.eplPassword}</div></div></div></section></div></main>};
