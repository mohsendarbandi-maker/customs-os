import React,{useEffect,useState} from 'react';
import {ArrowRight,Loader2,Plus,Save,Trash2} from 'lucide-react';
import {Link} from 'react-router-dom';
import {supabase} from '../lib/supabase';

type Rule={id?:string;rule_name:string;hs_prefix:string;cargo_keyword:string;permit_type:string;issuing_authority:string;required:boolean;priority:string;note:string;is_active:boolean};
const empty:Rule={rule_name:'',hs_prefix:'',cargo_keyword:'',permit_type:'',issuing_authority:'',required:true,priority:'100',note:'',is_active:true};
export const PermitRulesPage:React.FC=()=>{const[rules,setRules]=useState<Rule[]>([]),[form,setForm]=useState<Rule>(empty),[busy,setBusy]=useState(false),[message,setMessage]=useState('');const load=async()=>{const{data,error}=await supabase.from('permit_rules').select('id,rule_name,hs_prefix,cargo_keyword,permit_type,issuing_authority,required,priority,note,is_active').order('priority').order('rule_name');if(error)setMessage(error.message);setRules((data||[]) as Rule[])};useEffect(()=>{load()},[]);const save=async()=>{if(!form.rule_name.trim()||(!form.hs_prefix.trim()&&!form.cargo_keyword.trim())||!form.permit_type.trim()||!form.issuing_authority.trim())return setMessage('نام قاعده، HS یا کلیدواژه، نوع مجوز و مرجع صادرکننده الزامی است.');setBusy(true);try{const payload={rule_name:form.rule_name.trim(),hs_prefix:form.hs_prefix.trim()||null,cargo_keyword:form.cargo_keyword.trim()||null,permit_type:form.permit_type.trim(),issuing_authority:form.issuing_authority.trim(),required:form.required,priority:Number(form.priority)||100,note:form.note.trim()||null,is_active:form.is_active};const{error}=form.id?await supabase.from('permit_rules').update(payload).eq('id',form.id):await supabase.from('permit_rules').insert(payload);if(error)throw error;setForm(empty);setMessage(form.id?'قاعده ویرایش شد.':'قاعده ثبت شد.');await load()}catch(e:any){setMessage(e?.message||'خطا در ذخیره قاعده')}finally{setBusy(false)}};const edit=(r:Rule)=>setForm({...r,priority:String(r.priority)});const remove=async(id:string)=>{if(!window.confirm('این قاعده حذف شود؟'))return;setBusy(true);try{const{error}=await supabase.from('permit_rules').delete().eq('id',id);if(error)throw error;setMessage('قاعده حذف شد.');await load()}catch(e:any){setMessage(e?.message||'حذف انجام نشد')}finally{setBusy(false)}};return <main className="min-h-screen bg-slate-950 text-slate-100 p-5 md:p-8" dir="rtl"><div className="max-w-7xl mx-auto"><header className="flex items-center justify-between gap-4 mb-6"><div><h1 className="text-2xl font-black">موتور قواعد مجوز / Permit Rules Engine</h1><p className="text-sm text-slate-400 mt-2">قواعد قابل تنظیم برای پیشنهاد مجوز بر اساس HS و شرح کالا؛ الزام نهایی باید با مقررات جاری کنترل شود.</p></div><Link to="/" className="px-4 py-2 rounded-xl border border-slate-700 bg-slate-900"><ArrowRight className="inline ml-2" size={16}/> صفحه اصلی</Link></header>{message&&<div className="mb-4 p-3 rounded-xl bg-slate-900 border border-slate-800 text-sm">{message}</div>}<section className="bg-slate-900 border border-slate-800 rounded-2xl p-5 mb-5"><div className="grid md:grid-cols-2 lg:grid-cols-4 gap-4">{[['rule_name','نام قاعده'],['hs_prefix','پیشوند HS'],['cargo_keyword','کلیدواژه شرح کالا'],['permit_type','نوع مجوز'],['issuing_authority','مرجع صادرکننده'],['priority','اولویت'],['note','یادداشت']].map(([k,l])=><label key={k} className="text-xs text-slate-400">{l}<input value={(form as any)[k]} onChange={e=>setForm(p=>({...p,[k]:e.target.value}))} className="mt-1 w-full bg-slate-950 border border-slate-800 rounded-xl px-3 py-2.5" dir={['hs_prefix','priority'].includes(k)?'ltr':'rtl'}/></label>)}<label className="text-xs text-slate-400 flex items-center gap-2 pt-6"><input type="checkbox" checked={form.required} onChange={e=>setForm(p=>({...p,required:e.target.checked}))}/> مجوز لازم است</label><label className="text-xs text-slate-400 flex items-center gap-2 pt-6"><input type="checkbox" checked={form.is_active} onChange={e=>setForm(p=>({...p,is_active:e.target.checked}))}/> فعال</label></div><div className="mt-4 flex gap-2"><button onClick={save} disabled={busy} className="px-6 py-3 rounded-xl bg-violet-700 font-bold">{busy?<Loader2 className="inline ml-2 animate-spin" size={16}/>:form.id?<Save className="inline ml-2" size={16}/>:<Plus className="inline ml-2" size={16}/>} {form.id?'ذخیره تغییرات':'افزودن قاعده'}</button>{form.id&&<button onClick={()=>setForm(empty)} className="px-5 py-3 rounded-xl border border-slate-700">لغو و قاعده جدید</button>}</div></section><section className="bg-slate-900 border border-slate-800 rounded-2xl p-5"><div className="overflow-auto"><table className="w-full text-sm"><thead><tr className="text-slate-500 border-b border-slate-800"><th className="p-2">قاعده</th><th>HS</th><th>کلیدواژه</th><th>مجوز</th><th>مرجع</th><th>اولویت</th><th>فعال</th><th></th></tr></thead><tbody>{rules.length===0?<tr><td colSpan={8} className="p-5 text-slate-500">قاعده‌ای ثبت نشده.</td></tr>:rules.map(r=><tr key={r.id} className="border-b border-slate-900"><td className="p-2 font-bold">{r.rule_name}</td><td dir="ltr">{r.hs_prefix||'—'}</td><td>{r.cargo_keyword||'—'}</td><td>{r.permit_type}</td><td>{r.issuing_authority}</td><td dir="ltr">{r.priority}</td><td>{r.is_active?'بله':'خیر'}</td><td className="flex gap-3 p-2"><button onClick={()=>edit(r)} className="text-cyan-300">ویرایش</button><button onClick={()=>remove(r.id!)} className="text-red-300"><Trash2 size={16}/></button></td></tr>)}</tbody></table></div></section></div></main>};
